apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flink.yaml
  - spark.yaml

# Base configuration - no image transformations here
# Image registry selection is handled by overlays
